import styled from "styled-components"

export const InputForm = styled.input`

    padding: .7rem;
    border-radius: 30px;
    width: 90%;
    margin-bottom: 0.5rem;
    font-weight: bold;

    &:focus{
        box-shadow: 0 0 0 0;
        border: 0 none;
        outline: 0;
    }
`